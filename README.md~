# NLU-Language-Model

1. Generating corpus files and getting counts is required for token generation.
2. knn_tri.py in kneser-ney trains trigram model with kneser-ney.
3. runall.py in katz generates counts form corpus.
